[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = 
    --cov=.
    --cov-report=term-missing
    --cov-report=html
    -v
    --tb=short
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning 